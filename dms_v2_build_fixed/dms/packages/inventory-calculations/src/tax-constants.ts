/**
 * Single canonical HST rate for all inventory tax lines (purchase, cost-side, sell base).
 * Changing this value updates every dependent calculation.
 */
export const INVENTORY_HST_PERCENT_POINTS = 13;
